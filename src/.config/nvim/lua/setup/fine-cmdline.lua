require('fine-cmdline').setup({
	popup = {
		position = {
			row = '50%',
			col = '50%',
		},
	}
})

-- local map  = require('../utils.lua').map
-- map("n", "<CR>", ":FineCmdline", { silent = true })
